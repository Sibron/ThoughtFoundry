VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGc...

# Edge function secrets (set via `supabase secrets set ...`, NOT in this file
# and NOT prefixed with VITE_ — they must never reach the browser).
#
# ANTHROPIC_API_KEY=sk-ant-...           # required for all AI features
#
# Embeddings need no key: they use the Supabase Edge runtime's built-in
# gte-small model (384 dims, zero cost).
